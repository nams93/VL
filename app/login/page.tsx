import { LoginForm } from "@/components/login-form"

export const metadata = {
  title: "Connexion - GPIS",
  description: "Connectez-vous au système de gestion GPIS",
}

export default function LoginPage() {
  return <LoginForm />
}
